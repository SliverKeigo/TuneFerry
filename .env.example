# ============================================================
# TuneFerry — Environment Variables Example
# Copy this file to `.env` at the repo root.
# Next.js auto-loads .env when running `next dev` / `next build`.
# Never commit `.env`, any `.p8` private-key file, or Spotify secrets.
# ============================================================

# ---------- Apple Music Developer Token ----------
# Paste a pre-generated Developer Token here. The catalog search APIs
# return it verbatim and never touch the signing path.
# - WebPlay-scraped tokens (free): scrape from https://beta.music.apple.com
# - Self-signed (Apple Developer Program): use the keys below
APPLE_MUSIC_DEVELOPER_TOKEN=

# Optional self-sign mode. Requires an Apple Developer Program subscription.
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=
APPLE_PRIVATE_KEY_PATH=
APPLE_TOKEN_TTL_SECONDS=15777000

# ---------- Spotify Web API ----------
# Register an app at https://developer.spotify.com/dashboard
# Both required for both Client Credentials AND Authorization Code flows.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# OAuth redirect URI for Authorization Code flow.
# In dev: http://localhost:3000/api/spotify/auth/callback
# In production: https://your-domain.com/api/spotify/auth/callback
# Whatever you set, register the same string in your Spotify app's
# Redirect URIs whitelist.
SPOTIFY_REDIRECT_URI=http://localhost:3000/api/spotify/auth/callback

# Secret used to sign the OAuth state token (CSRF protection).
# Generate with: openssl rand -base64 32
SPOTIFY_STATE_SECRET=

# ---------- Client (Next.js — prefix NEXT_PUBLIC_ to expose to browser) ----------
# Initial Apple Music storefront the app boots with (e.g. us, jp, hk, tw, gb).
NEXT_PUBLIC_DEFAULT_STOREFRONT=us
